# Server-side only (read by api/news.ts). Never prefix with VITE_ or it would ship to the browser.
# Copy this file to .env.local (git-ignored) and fill it in.
# Get a key at https://gnews.io
GNEWS_API_KEY=
