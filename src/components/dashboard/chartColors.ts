/** One hue per kind of measure, from the app's chart palette: volume, quality, time and cost, failure. */
export const CHART_COLORS = {
  volume: "var(--chart-1)",
  quality: "var(--chart-3)",
  effort: "var(--chart-2)",
  failure: "var(--chart-8)",
} as const
